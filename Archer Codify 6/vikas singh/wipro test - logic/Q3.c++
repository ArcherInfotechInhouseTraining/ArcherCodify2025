

// . Array Rotation: This challenge is about moving numbers around in a list. It's useful for rearranging data.
// he Problem You have a list of numbers. You need to move the numbers to the right by a ertain amount. The user will tell you how many times.  Right the code in c++ 


#include <iostream>
using namespace std;
int main()
{
       int n, rotations;
       // Input the size Array
       cout<<" Enter the the size of array";
       cin>>n;

       int [n] , rotated =
    return 0;
}

































// #include <iostream>
// using namespace std;

// int main() {
//     int n, rotations;

//     // Input the size of the array
//     cout << "Enter the size of the array: ";
//     cin >> n;

//     int arr[n], rotated[n];

//     // Input the array elements
//     cout << "Enter the elements of the array: ";
//     for (int i = 0; i < n; i++) {
//         cin >> arr[i];
//     }

//     // Input the number of rotations
//     cout << "Enter the number of rotations: ";
//     cin >> rotations;

//     // Normalize rotations if it is greater than the array size
//     rotations = rotations % n;

//     // Rotate the array
//     for (int i = 0; i < n; i++) {
//         rotated[(i + rotations) % n] = arr[i];
//     }

//     // Print the rotated array
//     cout << "Rotated array: ";
//     for (int i = 0; i < n; i++) {
//         cout << rotated[i] << " ";
//     }
//     cout << endl;

//     return 0;
// }
